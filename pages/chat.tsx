import React from 'react';
import ChatView from '../components/chatView';

const Chat: React.FC = () => {
    return (
        <div style={{ height: '100vh' }}>
            <ChatView />
        </div>
    );
};

export default Chat;
